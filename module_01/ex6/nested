a nested class (or inner class) is a class defined inside another class

class OuterClass:
    """Outer/Container class"""
    
    class InnerClass:
        """Inner/Nested class"""
        pass

lets try this

we have university -> deperments -> classes

----------------------------------------------------------------------------
class university:
    def ini(self, name)
        //self.name --> name of universt: "harvard", "moulay smail", ...
	//self.deparments = []; <--  a list of deprments that exist in this uni
    --------------------------------------------------
    class department:
	def ini(self, department_name, head)
		self.departmetn_name = d_n
		self.head = head
		self.classes = []

    	def add_course():
		def ini(self, course)
			self.courses.append(course)

	def display():
		return f"{dpr.name} - head: self.head"
     --------------------------------------------------


||---> self.classes = []	==> does it equal lists??
||---> whats this exactly?!


how does this nested class works?



-> we have a university, which is general! doesnt contain anything at all!
-> then we have, deparemetnts that contain department name and heads
-> and lastly we have courses that each department provides


we start off by creating a university class that general

1- we store in a vraible object

uni = university("Harvard")

uni ==> is now an object that contains the university and nested class for department!

this means that, the department class can be used as an object to create the department name and the head


uni.department("name of depratment", "head name")

















